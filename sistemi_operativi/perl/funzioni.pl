#!/usr/bin/perl



#join(<separatore>, <lista>) unisce tutti gli elementi della lista separati da separatore

#chomp (<stringa>) data una stringa rimuove tutti i caratteri \n da esso

#next è il continue di java
#last esce dal loop (come break di java)
#redo inizia di nuovo il loop senza controllare la condizione